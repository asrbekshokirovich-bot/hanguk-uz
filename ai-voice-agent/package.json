{
  "name": "hanguk-ai-voice-agent",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI voice agent: answers inbound and places outbound calls via Azure Communication Services, bridged to the Azure OpenAI Realtime API, logged into the Hanguk CRM.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.mjs",
    "dev": "node --env-file=.env server.mjs"
  },
  "dependencies": {
    "@azure/communication-call-automation": "^1.3.0",
    "express": "^4.19.2",
    "ws": "^8.18.0"
  }
}
