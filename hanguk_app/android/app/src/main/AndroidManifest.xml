<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Play policy block (2026-08-03, version code 2040): "Use alternative
         system pickers for photos / videos". Apps targeting API 33+ may only
         hold READ_MEDIA_IMAGES / READ_MEDIA_VIDEO if a system picker cannot
         provide core functionality. Ours can:

           * The only file access in the app is document upload
             (features/documents/presentation/documents_tab.dart), which calls
             FilePicker with FileType.custom. On Android that goes through the
             Storage Access Framework — the user picks one file and the app
             receives a grant for it. No storage permission is involved at any
             API level.
           * Nothing reads the media store, enumerates the gallery, or asks for
             these permissions at runtime. `image_picker` is in pubspec but is
             not imported anywhere; permission_handler is used only by
             packages/vapi, and only for the microphone.

         So the declarations below were doing nothing except blocking release.
         node="remove" rather than plain deletion because plugin manifests
         (image_picker_android, permission_handler_android) merge these in on
         their own — deleting our copies alone would leave them in the merged
         manifest and Play would flag the build again.

         If a feature ever genuinely needs gallery access, reach for the
         Android photo picker (PickVisualMedia) first; it needs no permission.
         Only if that is provably insufficient should these come back, and then
         the Play Console declaration under App content has to come back too. -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" tools:node="remove" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" tools:node="remove" />
    <!-- Superseded by the READ_MEDIA_* split on API 33+, so this is inert
         there. Capped explicitly so it cannot be read as a broad storage
         request under the same policy. SAF does not need it below 33 either;
         it stays only because plugin manifests declare it and capping is
         cheaper than fighting the merge. -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:replace="android:maxSdkVersion" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:label="Hanguk"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Audit K3 (2026-05-11): removed orphan `com.kakao.sdk.AppKey`
             meta-data that was left over from an abandoned
             `kakao_maps_flutter` SDK attempt. We do not ship Kakao
             Login / Share / Channel / AlimTalk; no Kakao native SDK is
             in pubspec to read this meta-data. If we ever ship Kakao
             Login, this meta-data + the `kakao{NATIVE_KEY}` OAuth
             intent-filter need to be added back together.
             See docs/audits/kakaotalk_audit_2026-05-11.md §3 K3. -->

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
