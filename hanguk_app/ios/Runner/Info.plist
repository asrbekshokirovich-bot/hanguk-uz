<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Hanguk App needs access to your photo library to upload documents and certificates.</string>
	<key>NSCameraUsageDescription</key>
	<string>Hanguk App needs camera access to quickly scan new documents and verify your identity.</string>
	<key>NSSpeechRecognitionUsageDescription</key>
	<string>Hanguk App needs speech recognition to transcribe your spoken answers during AI mock interviews.</string>
	<key>NSMicrophoneUsageDescription</key>
	<string>Hanguk App needs microphone access to record your voice during AI mock interviews.</string>
	<!-- Export compliance. Answering here means App Store Connect stops asking
	     on every single upload, which is friction the release process does not
	     need. `false` is the declaration that the app uses no non-exempt
	     encryption; the basis for it, so the next person can re-check rather
	     than trust it:

	       * All network traffic is HTTPS/TLS provided by the OS — exempt.
	       * `flutter_secure_storage` stores through the iOS Keychain — Apple's
	         own encryption, exempt.
	       * `crypto` is used for SHA-256 verification of downloaded artifacts.
	         Hashing is not encryption — exempt.
	       * `pointycastle` arrives transitively under `dart_jsonwebtoken` and
	         is used to verify JWT signatures. Digital signature and
	         authentication are named exemptions.

	     Nothing in the app implements or ships its own encryption algorithm.
	     If that changes — encrypting stored documents with a bundled cipher,
	     say — this becomes `true` and the annual self-classification report
	     applies. It is a legal declaration, so confirm it before the next
	     submission rather than inheriting it. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>CADisableMinimumFrameDurationOnPhone</key>
	<true/>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>Hanguk</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>Hanguk</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleSignature</key>
	<string>????</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
		<key>UISceneConfigurations</key>
		<dict>
			<key>UIWindowSceneSessionRoleApplication</key>
			<array>
				<dict>
					<key>UISceneClassName</key>
					<string>UIWindowScene</string>
					<key>UISceneConfigurationName</key>
					<string>flutter</string>
					<key>UISceneDelegateClassName</key>
					<string>$(PRODUCT_MODULE_NAME).SceneDelegate</string>
					<key>UISceneStoryboardFile</key>
					<string>Main</string>
				</dict>
			</array>
		</dict>
	</dict>
	<key>UIApplicationSupportsIndirectInputEvents</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
</dict>
</plist>
