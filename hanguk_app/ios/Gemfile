# Pinned so a release is reproducible: fastlane ships breaking changes in
# minor versions, and a release pipeline that silently upgrades itself the
# evening a submission is due is not a pipeline.
source "https://rubygems.org"

gem "fastlane", "~> 2.228"
