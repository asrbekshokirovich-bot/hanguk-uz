# ----------------------------------------------------------------------------
# Supabase credentials for the dev/ops scripts in this repo.
#
# These scripts use an ELEVATED key. Never hardcode it and never commit a real
# value. Copy this file to `.env` (already gitignored) and fill it in, then run
# the scripts with the variable available in your environment.
#
# Create a SECRET key in:
#   Supabase Dashboard -> Project Settings -> API Keys  (format: sb_secret_...)
# This replaces the old, leaked legacy `service_role` key.
# ----------------------------------------------------------------------------
SUPABASE_URL=https://lysjdtyanhdfphqyijsr.supabase.co
SUPABASE_SECRET_KEY=sb_secret_replace_me
